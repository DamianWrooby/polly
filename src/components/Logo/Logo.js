import React from 'react';
import classes from './Logo.module.css';

const Logo = () => {
  return (
    <div className={classes.Logo}>
      <a href="/">POLLY</a>
    </div>
  );
};

export default Logo;
